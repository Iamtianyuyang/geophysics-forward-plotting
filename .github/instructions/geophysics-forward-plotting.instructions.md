---
applyTo: "src/**/*.py,tests/**/*.py,examples/**/*.py,examples/**/*.yaml,skills/**/*.md"
---

Use physical coordinates and units for geophysical figures. Keep time/depth axes downward,
signed amplitude limits symmetric, and comparison color limits global. Read the relevant
canonical `skills/<name>/SKILL.md` before changing plotting behavior and update its tests.

